spring:
  application:
    name: lookie
  datasource:
    url: jdbc:mysql://localhost:3306/lookie
    username: root
    password: lonnie414?
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
  profiles:
    active: redis,oauth,s3
logging:
  level:
    org.example.lookie: DEBUG